// Control flow - conditionals, loops are a part of it
// It is the specific order in which program executes its statements (lines of code)

// Conditionals - if, else, else if

#include <iostream>

using namespace std;

int main() {

    // below is a sample - it does not compile!

    if(/*condition - either a boolean value
        or an expression giving us a boolean value
        as a result*/) {
            /* 
            code of the if-block 
            this code executes
            only if the condition is true
            */
    }

    return 0;
}